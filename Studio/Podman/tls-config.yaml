http:
  middlewares:
    web-to-websecure:
      redirectScheme:
        scheme: https
        port: '{{ env "INGRESS_HTTPS_PORT" }}'
        permanent: true

tls:
  stores:
    default:
      defaultCertificate:
        certFile: /etc/certs/cert.cer
        keyFile: /etc/certs/cert.key
